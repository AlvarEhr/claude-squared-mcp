[project]
name = "claude-squared-extension"
version = "0.1.0"
description = "MCPB-bundled claude-squared"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "filelock>=3.13",
    "pydantic>=2.5",
]
